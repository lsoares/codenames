#!/bin/sh
# Block a push whose build or tests fail, so main never goes red. Bypass in a
# genuine emergency with `git push --no-verify`.
set -e

echo "pre-push: format…"
npm run format:check

echo "pre-push: typecheck + build…"
npm run build

echo "pre-push: tests…"
npm test
